import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
    margin: 10,
    backgroundColor: '#677791',
    borderRadius: 10,
  },
  text: {
    color: '#fff',
    fontSize: 16,
  },
});

export default styles;
